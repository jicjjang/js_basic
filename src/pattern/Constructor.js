// This pattern include Module Pattern, Reveal Module Pattern when used to ES5.
// (Module Paterrn == Closure.)

class Constructor {
  constructor() {
    let variable1 = null
    let variable2 = 1
  }

  // Es6 - Overloading is impassible
  // Ess6 - Overriding is passible.
  // Because, basically constructor is returned created null object when class haven't variables. (or functions)

  temp() {
    // ...
  }
}

export default Constructor;
